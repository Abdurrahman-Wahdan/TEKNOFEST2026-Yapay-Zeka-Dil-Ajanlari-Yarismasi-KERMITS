[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers --strict-config
asyncio_mode = auto

markers =
    unit: no network, no external services
    integration: needs a live vLLM host, Qdrant, and/or the banks' endpoints
    slow: takes more than a few seconds
