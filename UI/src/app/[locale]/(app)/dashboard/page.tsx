"use client";

import Page from "layouts/dashboard";

/** Vision UI's dashboard page, rendered unchanged inside the Vision UI shell. */
export default function DashboardRoute() {
  return <Page />;
}
